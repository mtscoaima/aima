================================================================================
카카오/RCS/네이버 메시지 발송 기능 구현 계획서
================================================================================
작성일: 2025-10-13
대상: messages/send 페이지 - 카카오/RCS/네이버 관련 전체 기능
범위:
  1. 메시지 보내기 탭 > 카카오/RCS/네이버 하위 탭
  2. 카카오/네이버/RCS 통합 관리 탭 (KakaoNaverRcsTab)


================================================================================
목차
================================================================================
1. 프로젝트 개요 및 현황
2. UI 구조 분석
3. 필요한 외부 API 및 환경변수
4. 데이터베이스 스키마
5. API 엔드포인트 설계
6. 단계별 구현 계획
7. 각 채널별 상세 구현 가이드
   7.1 카카오톡 (알림톡/친구톡/브랜드)
   7.2 RCS
   7.3 네이버 톡톡
8. 테스트 시나리오
9. 보안 및 에러 처리


================================================================================
1. 프로젝트 개요 및 현황
================================================================================

1.1 구현 대상
-------------
이 계획서는 SMS/LMS/MMS를 제외한 나머지 모든 메시징 채널을 다룹니다:

✅ 메시지 보내기 탭 (MessageSendTab) - 3개 하위 탭
   - 💬 카카오톡 (KakaoMessageContent)
     - 알림톡 (Alimtalk)
     - 친구톡 (Friendtalk)
     - 브랜드 메시지

   - 🔵 RCS (RcsMessageContent)
     - RCS 브랜드 연동
     - RCS 템플릿
     - RCS 메시지 버튼
     - RCS 슬라이드

   - 🟢 네이버 톡톡 (NaverTalkContent)
     - 네이버 스마트알림

✅ 카카오/네이버/RCS 탭 (KakaoNaverRcsTab)
   - 별도의 통합 관리 페이지

1.2 현재 상태
-------------
✅ UI 완성: 모든 컴포넌트 완전 구현
✅ 기존 SMS API: Naver SENS API 설정 완료
❌ 카카오 API: 미설정 (환경변수 없음)
❌ RCS API: 미설정 (환경변수 없음)
❌ 네이버 톡톡 API: 미설정 (환경변수 없음)
❌ 백엔드 로직: 전체 미구현
❌ 채널 연동: 미완료

1.3 구현 난이도
-------------
⚠️ 이 기능들은 SMS보다 훨씬 복잡합니다:

1. **외부 API 연동 필요**
   - 카카오 비즈니스 채널 연동
   - RCS Biz Center 연동
   - 네이버 톡톡 연동
   - 각각 별도의 승인/인증 절차 필요

2. **템플릿 관리 시스템**
   - 알림톡: 사전 승인된 템플릿만 사용 가능
   - 브랜드 메시지: 8가지 타입별 템플릿
   - RCS: 브랜드 등록 및 템플릿 승인
   - 네이버 톡톡: 네이버 스마트알림 템플릿

3. **이미지/미디어 처리**
   - 각 채널별 이미지 규격 상이
   - 파일 업로드 및 ID 발급
   - 슬라이드/캐러셀 구성

4. **버튼/액션 관리**
   - 웹링크, 앱링크, 전화걸기 등
   - 채널별 최대 버튼 수 제한

1.4 구현 전략
-------------
SMS와 달리 단계적 접근이 필요합니다:

Phase 1: API 연동 및 계정 설정 (2-3주)
  - 각 채널별 사업자 계정 등록
  - API Key 발급
  - 테스트 환경 구축

Phase 2: 기본 발송 기능 (2주)
  - 카카오 알림톡 (가장 단순)
  - 카카오 친구톡
  - 템플릿 관리

Phase 3: 고급 기능 (3주)
  - RCS 발송
  - 브랜드 메시지
  - 네이버 톡톡
  - 이미지/버튼/슬라이드


================================================================================
2. UI 구조 분석
================================================================================

2.1 카카오톡 (KakaoMessageContent.tsx)
--------------------------------------

하위 탭 3개:
  1. 알림톡 (alimtalk)
     - 카카오 채널 선택
     - 알림톡 템플릿 선택 (승인된 템플릿만)
     - 템플릿 내용 표시 (수정 불가)
     - 문구 치환 (변수 매칭)
     - 발송실패 시 문자대체발송 옵션

  2. 친구톡 (friendtalk)
     - 카카오 채널 선택
     - 메시지 내용 직접 입력 (최대 1,000자)
     - 치환문구 추가 (#[변수 A] 형식)
     - 이미지 첨부 (500KB 이하, 500x250px 이상)
     - 이미지 링크 URL (최대 100자)
     - 카카오톡 버튼 추가
     - 광고메시지 여부
     - 발송실패 시 문자대체발송 옵션

  3. 브랜드 메시지 (brand)
     - 카카오 채널 선택
     - 브랜드 템플릿 선택
     - 8가지 템플릿 타입:
       - 텍스트형
       - 이미지형
       - 와이드형
       - 와이드리스트형
       - 캐러셀피드형
       - 커머스형
       - 캐러셀커머스형
       - 프리미엄동영상 (비활성)
     - 수신대상 필터링:
       - 전체 (비활성)
       - 수신동의자만 (비활성)
       - 채널친구만 (활성)
     - 문구 치환

주요 상태:
```typescript
const [activeKakaoTab, setActiveKakaoTab] = useState("alimtalk");
const [templateContent, setTemplateContent] = useState("");
const [friendTalkContent, setFriendTalkContent] = useState("");
const [selectedTemplate, setSelectedTemplate] = useState<keyof typeof templateTypes>("wide");
const [showImageUpload, setShowImageUpload] = useState(false);
```


2.2 RCS (RcsMessageContent.tsx)
-------------------------------

구성 요소:
  - RCS 브랜드 선택
  - RCS 템플릿 선택 (없으면 직접 입력)
  - 제목 (선택, 최대 30자)
  - 메시지 내용 (최대 1,300자)
  - 치환문구 ({{변수}} 형식)
  - 이미지 첨부 (권장: 568x336px)
  - RCS 메시지 버튼 (최대 2개)
  - RCS 슬라이드 (좁게/넓게, 최대 5개)
  - 광고메시지 여부
  - 발송실패 시 문자대체발송 (기본 체크)
  - 메시지 공유가능여부

주요 상태:
```typescript
const [subjectLength, setSubjectLength] = useState(0);
const [messageLength, setMessageLength] = useState(0);
const [messageContent, setMessageContent] = useState("");
const [selectedSlideType, setSelectedSlideType] = useState("none");
const [showImageUpload, setShowImageUpload] = useState(false);
```


2.3 네이버 톡톡 (NaverTalkContent.tsx)
--------------------------------------

구성 요소:
  - 안내 메시지: [네이버 스마트알림 발송방법 및 수신확인 문의 : 1577-1603]
  - 네이버톡 선택 (드롭다운)
  - 템플릿 선택 (드롭다운)
  - 템플릿 내용 표시 (수정 불가)
  - 문구 치환
  - 발송실패 시 문자대체발송 (기본 체크)

주요 상태:
```typescript
const [templateContent, setTemplateContent] = useState("");
```


================================================================================
3. 필요한 외부 API 및 환경변수
================================================================================

3.1 카카오 비즈니스 메시지 API
----------------------------
필요한 설정:
```env
# 카카오 알림톡/친구톡/브랜드
KAKAO_BIZM_API_KEY=your_api_key
KAKAO_BIZM_SENDER_KEY=your_sender_key
KAKAO_BIZM_API_URL=https://api.kakao.com/v2/alimtalk/send

# 카카오 채널 정보
KAKAO_CHANNEL_ID=your_channel_id
KAKAO_CHANNEL_UUID=your_channel_uuid
```

연동 절차:
1. 카카오 비즈니스 채널 개설 (https://business.kakao.com/)
2. 카카오싱크 비즈니스 가입
3. 발신 프로필 등록 및 심사
4. API Key 발급
5. 알림톡 템플릿 등록 및 심사 (2-3일 소요)


3.2 RCS (Rich Communication Services) API
-----------------------------------------
필요한 설정:
```env
# RCS Biz Center
RCS_API_KEY=your_api_key
RCS_BRAND_ID=your_brand_id
RCS_CHATBOT_ID=your_chatbot_id
RCS_API_URL=https://rcs-api.example.com

# 이통사별 RCS (예: KT)
KT_RCS_API_KEY=your_kt_key
SK_RCS_API_KEY=your_sk_key
LG_RCS_API_KEY=your_lg_key
```

연동 절차:
1. RCS Biz Center 가입 (https://www.rcsbizcenter.com/)
2. 브랜드 등록 및 심사 (1-2주 소요)
3. 챗봇 생성 및 승인
4. 메시지베이스 설정
5. API 연동 테스트


3.3 네이버 톡톡 (스마트알림) API
------------------------------
필요한 설정:
```env
# 네이버 톡톡
NAVER_TALKTALK_API_KEY=your_api_key
NAVER_TALKTALK_SECRET_KEY=your_secret_key
NAVER_TALKTALK_PARTNER_ID=your_partner_id
NAVER_TALKTALK_API_URL=https://talk.naver.com/api/v1
```

연동 절차:
1. 네이버 톡톡 파트너 가입 (https://talk.naver.com/)
2. 서비스 등록 및 심사
3. 스마트알림 템플릿 등록
4. API Key 발급


3.4 중계 서비스 활용 (권장)
-------------------------
⚠️ 직접 연동보다 중계 서비스 권장!

이유:
- 각 채널별 복잡한 인증 절차
- 템플릿 심사 프로세스
- API 명세 변경 대응
- 발송 실패 처리
- 통계 및 리포팅

추천 중계 서비스:
1. **NHN Cloud (구 TOAST)**
   - 카카오/RCS/네이버 통합 지원
   - 웹훅 및 콜백 지원
   - 상세한 문서화
   - https://www.toast.com/kr/service/notification

2. **솔라피 (Solapi)**
   - 카카오/RCS 지원
   - 간편한 API
   - https://solapi.com

3. **알리고 (Aligo)**
   - 카카오톡 전문
   - https://smartsms.aligo.in

중계 서비스 사용 시:
```env
# NHN Cloud 예시
NHN_CLOUD_APPKEY=your_appkey
NHN_CLOUD_SECRET_KEY=your_secret
NHN_CLOUD_KAKAO_SENDER_KEY=your_sender
```


================================================================================
4. 데이터베이스 스키마
================================================================================

4.1 카카오 채널 정보 (kakao_channels)
------------------------------------
```sql
CREATE TABLE kakao_channels (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  channel_id VARCHAR(100) NOT NULL,
  channel_name VARCHAR(200),
  sender_key VARCHAR(100),
  status VARCHAR(20) DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_kakao_channels_user_id ON kakao_channels(user_id);
```


4.2 카카오 템플릿 (kakao_templates)
----------------------------------
```sql
CREATE TABLE kakao_templates (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  channel_id INTEGER REFERENCES kakao_channels(id),
  template_type VARCHAR(20) NOT NULL, -- 'alimtalk', 'friendtalk', 'brand'
  template_code VARCHAR(100), -- 알림톡 템플릿 코드
  template_name VARCHAR(200),
  template_content TEXT,
  template_variables JSONB, -- 변수 목록
  buttons JSONB, -- 버튼 정보
  status VARCHAR(20) DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_kakao_templates_user_id ON kakao_templates(user_id);
CREATE INDEX idx_kakao_templates_type ON kakao_templates(template_type);
```


4.3 RCS 브랜드 정보 (rcs_brands)
-------------------------------
```sql
CREATE TABLE rcs_brands (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  brand_id VARCHAR(100) NOT NULL,
  brand_name VARCHAR(200),
  chatbot_id VARCHAR(100),
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_rcs_brands_user_id ON rcs_brands(user_id);
```


4.4 RCS 템플릿 (rcs_templates)
-----------------------------
```sql
CREATE TABLE rcs_templates (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  brand_id INTEGER REFERENCES rcs_brands(id),
  template_name VARCHAR(200),
  template_content TEXT,
  message_base_id VARCHAR(100),
  buttons JSONB,
  slides JSONB,
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_rcs_templates_user_id ON rcs_templates(user_id);
```


4.5 네이버 톡톡 정보 (naver_talks)
---------------------------------
```sql
CREATE TABLE naver_talks (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  partner_id VARCHAR(100),
  service_id VARCHAR(100),
  service_name VARCHAR(200),
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_naver_talks_user_id ON naver_talks(user_id);
```


4.6 네이버 톡톡 템플릿 (naver_talk_templates)
-------------------------------------------
```sql
CREATE TABLE naver_talk_templates (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  service_id INTEGER REFERENCES naver_talks(id),
  template_code VARCHAR(100),
  template_name VARCHAR(200),
  template_content TEXT,
  template_variables JSONB,
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_naver_talk_templates_user_id ON naver_talk_templates(user_id);
```


4.7 통합 메시지 발송 로그 (multi_channel_message_logs)
-----------------------------------------------------
```sql
CREATE TABLE multi_channel_message_logs (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  channel_type VARCHAR(20) NOT NULL, -- 'kakao_alimtalk', 'kakao_friendtalk', 'kakao_brand', 'rcs', 'naver_talk'
  to_number VARCHAR(20) NOT NULL,
  to_name VARCHAR(100),
  message_content TEXT NOT NULL,
  template_id INTEGER,
  sent_at TIMESTAMP,
  status VARCHAR(20) NOT NULL, -- 'sent', 'failed', 'pending'
  error_message TEXT,
  credit_used INTEGER,
  metadata JSONB, -- 채널별 추가 정보
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_multi_channel_logs_user_id ON multi_channel_message_logs(user_id);
CREATE INDEX idx_multi_channel_logs_channel ON multi_channel_message_logs(channel_type);
CREATE INDEX idx_multi_channel_logs_sent_at ON multi_channel_message_logs(sent_at);
```


================================================================================
5. API 엔드포인트 설계
================================================================================

5.1 카카오톡 API
---------------

📍 POST /api/kakao/channels
- 카카오 채널 등록

📍 GET /api/kakao/channels
- 카카오 채널 목록 조회

📍 POST /api/kakao/templates
- 알림톡 템플릿 등록

📍 GET /api/kakao/templates
- 템플릿 목록 조회

📍 POST /api/kakao/send/alimtalk
- 알림톡 발송

📍 POST /api/kakao/send/friendtalk
- 친구톡 발송

📍 POST /api/kakao/send/brand
- 브랜드 메시지 발송

�� POST /api/kakao/upload/image
- 이미지 업로드


5.2 RCS API
----------

📍 POST /api/rcs/brands
- RCS 브랜드 등록

📍 GET /api/rcs/brands
- RCS 브랜드 목록 조회

📍 POST /api/rcs/templates
- RCS 템플릿 등록

📍 GET /api/rcs/templates
- RCS 템플릿 목록 조회

📍 POST /api/rcs/send
- RCS 메시지 발송

📍 POST /api/rcs/upload/media
- RCS 미디어 업로드


5.3 네이버 톡톡 API
-----------------

📍 POST /api/naver-talk/services
- 네이버 톡톡 서비스 등록

📍 GET /api/naver-talk/services
- 네이버 톡톡 서비스 목록

📍 POST /api/naver-talk/templates
- 스마트알림 템플릿 등록

📍 GET /api/naver-talk/templates
- 템플릿 목록 조회

📍 POST /api/naver-talk/send
- 스마트알림 발송


================================================================================
6. 단계별 구현 계획
================================================================================

Phase 1: 기반 작업 (1주)
------------------------
[ ] 중계 서비스 선정 및 계약
[ ] API Key 발급
[ ] 테스트 계정 생성
[ ] DB 테이블 생성
[ ] 기본 API 구조 설계


Phase 2: 카카오 알림톡 (1-2주)
-----------------------------
[ ] 카카오 채널 등록 API
[ ] 카카오 채널 관리 UI 연동
[ ] 알림톡 템플릿 등록 API
[ ] 알림톡 템플릿 선택 UI 연동
[ ] 알림톡 발송 API
[ ] 발송 테스트
[ ] 에러 처리 및 대체발송


Phase 3: 카카오 친구톡 (1주)
---------------------------
[ ] 친구톡 발송 API
[ ] 이미지 업로드 처리
[ ] 버튼 추가 기능
[ ] UI 연동
[ ] 테스트


Phase 4: 카카오 브랜드 메시지 (1주)
---------------------------------
[ ] 브랜드 템플릿 관리
[ ] 8가지 템플릿 타입 처리
[ ] 수신대상 필터링
[ ] UI 연동
[ ] 테스트


Phase 5: RCS (2-3주)
-------------------
[ ] RCS 브랜드 등록
[ ] RCS 템플릿 관리
[ ] RCS 발송 API
[ ] 버튼/슬라이드 처리
[ ] 미디어 업로드
[ ] UI 연동
[ ] 테스트


Phase 6: 네이버 톡톡 (1주)
-------------------------
[ ] 네이버 톡톡 서비스 등록
[ ] 스마트알림 템플릿 관리
[ ] 스마트알림 발송 API
[ ] UI 연동
[ ] 테스트


Phase 7: 통합 및 최적화 (1주)
----------------------------
[ ] 통합 발송 로그
[ ] 통계 대시보드
[ ] 에러 모니터링
[ ] 성능 최적화
[ ] 문서화


================================================================================
7. 각 채널별 상세 구현 가이드
================================================================================

7.1 카카오 알림톡 구현
--------------------

알림톡 특징:
- 사전 승인된 템플릿만 사용 가능
- 템플릿 내용 수정 불가
- 변수 치환만 가능
- 발송 성공률 높음
- 광고 아님 (수신거부 불필요)

구현 순서:
1. 카카오 채널 연동
2. 템플릿 심사 신청
3. 승인된 템플릿 DB 저장
4. 발송 시 템플릿 코드 + 변수 전달
5. 실패 시 SMS 대체발송

코드 예시 (src/app/api/kakao/send/alimtalk/route.ts):
```typescript
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { templateCode, recipients, variables } = body;

    // JWT 인증
    const userId = await getUserIdFromToken(request);
    if (!userId) {
      return NextResponse.json({ error: '인증 실패' }, { status: 401 });
    }

    // 템플릿 조회
    const template = await getTemplate(templateCode);
    if (!template || template.status !== 'approved') {
      return NextResponse.json({ error: '승인되지 않은 템플릿' }, { status: 400 });
    }

    // 카카오 API 호출 (중계 서비스)
    const results = [];
    for (const recipient of recipients) {
      const result = await sendAlimtalk({
        senderKey: template.sender_key,
        templateCode: templateCode,
        recipientNo: recipient.phone_number,
        templateParameter: variables
      });

      results.push({
        recipient: recipient.phone_number,
        success: result.success,
        messageId: result.requestId
      });

      // 로그 저장
      await saveMessageLog({
        userId,
        channelType: 'kakao_alimtalk',
        toNumber: recipient.phone_number,
        messageContent: template.template_content,
        status: result.success ? 'sent' : 'failed',
        creditUsed: 15 // 알림톡 단가
      });
    }

    return NextResponse.json({ success: true, results });
  } catch (error) {
    console.error('알림톡 발송 오류:', error);
    return NextResponse.json({ error: '발송 실패' }, { status: 500 });
  }
}
```


7.2 카카오 친구톡 구현
--------------------

친구톡 특징:
- 내용 자유 작성
- 친구 추가한 사용자에게만 발송
- 이미지 첨부 가능
- 버튼 추가 가능
- 광고성 메시지 가능

구현 포인트:
1. 채널 친구 확인 필요
2. 이미지 업로드 → 카카오 이미지 서버
3. 버튼 JSON 구조 생성
4. 광고 표기 자동 처리


7.3 RCS 구현
-----------

RCS 특징:
- 가장 복잡한 채널
- 브랜드 심사 필수
- 리치 미디어 지원
- 캐러셀/슬라이드 지원
- 버튼 액션 다양

구현 포인트:
1. 브랜드 등록 → 심사 대기 (1-2주)
2. 메시지베이스 설정
3. 미디어 업로드
4. 슬라이드 JSON 생성
5. 대체발송 필수


7.4 네이버 톡톡 구현
------------------

네이버 톡톡 특징:
- 알림톡과 유사
- 템플릿 사전 등록
- 발송 단가 저렴
- 네이버 앱 푸시

구현 포인트:
1. 파트너 가입
2. 템플릿 등록
3. 변수 치환
4. 대체발송


================================================================================
8. 테스트 시나리오
================================================================================

8.1 카카오 알림톡 테스트
-----------------------
[ ] 채널 등록
[ ] 템플릿 등록 및 심사
[ ] 템플릿 선택
[ ] 변수 매칭
[ ] 발송 테스트
[ ] 발송 실패 시 SMS 대체발송
[ ] 로그 확인

8.2 카카오 친구톡 테스트
-----------------------
[ ] 채널 친구 확인
[ ] 메시지 작성
[ ] 이미지 업로드
[ ] 버튼 추가
[ ] 발송 테스트
[ ] 광고 표기 확인

8.3 RCS 테스트
-------------
[ ] 브랜드 등록
[ ] 템플릿 등록
[ ] 이미지/동영상 업로드
[ ] 슬라이드 구성
[ ] 버튼 추가
[ ] 발송 테스트
[ ] 대체발송 확인

8.4 네이버 톡톡 테스트
--------------------
[ ] 서비스 등록
[ ] 템플릿 등록
[ ] 변수 매칭
[ ] 발송 테스트
[ ] 대체발송 확인


================================================================================
9. 보안 및 에러 처리
================================================================================

9.1 보안
--------
- API Key 암호화 저장
- 템플릿 승인 상태 검증
- 사용자별 채널 격리
- 발송 권한 검증

9.2 에러 처리
------------
- 채널 연동 실패
- 템플릿 심사 반려
- 발송 실패 (친구 아님, 차단 등)
- API 오류
- 대체발송 트리거


================================================================================
10. 예상 비용
================================================================================

10.1 메시지 단가 (참고)
---------------------
- 카카오 알림톡: 8-15원
- 카카오 친구톡: 13-18원
- 카카오 브랜드: 20-25원
- RCS: 15-30원
- 네이버 톡톡: 6-10원

※ 중계 서비스 이용 시 단가 상이


10.2 초기 비용
------------
- 카카오 채널 개설: 무료
- RCS 브랜드 등록: 무료 (심사만 통과하면)
- 네이버 톡톡 파트너: 무료
- 중계 서비스 월 사용료: 10만원~


================================================================================
11. 권장 구현 순서
================================================================================

✅ 1순위: 카카오 알림톡
   - 가장 많이 사용
   - 구현 난이도 중
   - 발송 성공률 높음

✅ 2순위: 카카오 친구톡
   - 알림톡과 연계
   - 이미지 활용 가능

⚠️ 3순위: RCS
   - 구현 복잡
   - 심사 기간 길음
   - 최신 기술

⚠️ 4순위: 네이버 톡톡
   - 사용 비중 낮음
   - 네이버 사용자만

⚠️ 5순위: 카카오 브랜드
   - 고급 기능
   - 템플릿 제작 복잡


================================================================================
12. 최종 체크리스트
================================================================================

구현 전:
--------
[ ] 중계 서비스 계약 완료?
[ ] 카카오 비즈니스 채널 개설?
[ ] RCS 브랜드 심사 신청?
[ ] 네이버 톡톡 파트너 가입?
[ ] API Key 발급 완료?
[ ] 테스트 계정 준비?

구현 중:
--------
[ ] DB 테이블 생성?
[ ] API 엔드포인트 구현?
[ ] 템플릿 관리 시스템?
[ ] 이미지 업로드 처리?
[ ] 발송 로직 구현?
[ ] UI 연동?

구현 후:
--------
[ ] 전체 채널 발송 테스트?
[ ] 대체발송 동작 확인?
[ ] 에러 처리 검증?
[ ] 로그 저장 확인?
[ ] 통계 대시보드?
[ ] 문서화 완료?


================================================================================
13. 참고 자료
================================================================================

공식 문서:
- 카카오 비즈니스 메시지: https://business.kakao.com/
- RCS Biz Center: https://www.rcsbizcenter.com/
- 네이버 톡톡: https://talk.naver.com/

중계 서비스:
- NHN Cloud: https://docs.toast.com/ko/Notification/KakaoTalk%20Bizmessage/ko/overview/
- 솔라피: https://docs.solapi.com/
- 알리고: https://smartsms.aligo.in/admin/api/spec.html


================================================================================
문서 끝
================================================================================

⚠️ 이 계획서는 SMS보다 훨씬 복잡합니다!
⚠️ 외부 API 연동 및 심사 절차가 필요합니다!
⚠️ 중계 서비스 활용을 강력히 권장합니다!

📌 구현 예상 기간: 최소 6-8주
📌 초기 비용: API 계약 + 중계 서비스 월 사용료
📌 난이도: ⭐⭐⭐⭐⭐ (매우 높음)

================================================================================
