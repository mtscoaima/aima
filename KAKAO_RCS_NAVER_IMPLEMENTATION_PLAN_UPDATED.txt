================================================================================
카카오/RCS/네이버 메시지 발송 기능 구현 계획서 (UI 기반 수정본)
================================================================================
작성일: 2025-10-15
수정일: 2025-10-15
대상: messages/send 페이지 - 카카오/RCS/네이버 관련 전체 기능
범위:
  1. 메시지 보내기 탭 (MessageSendTab) - 카카오/RCS/네이버 하위 탭 (발송용)
  2. 카카오/네이버/RCS 탭 (KakaoNaverRcsTab) - 채널/템플릿 관리용


================================================================================
목차
================================================================================
0. 🚨 시작하기 전 필수 이해사항 (반드시 읽어주세요!)
1. 프로젝트 개요 및 현황
2. UI 구조 완전 분석
3. 필요한 외부 API 및 환경변수
4. 데이터베이스 스키마
5. API 엔드포인트 설계
6. 단계별 구현 계획 (UI 기반)
7. 각 채널별 상세 구현 가이드
   7.1 카카오톡 (알림톡/친구톡/브랜드)
   7.2 RCS
   7.3 네이버 톡톡
8. 테스트 시나리오
9. 보안 및 에러 처리


================================================================================
0. 🚨 시작하기 전 필수 이해사항 (반드시 읽어주세요!)
================================================================================

⚠️ 이 섹션을 건너뛰면 잘못된 API를 사용하게 됩니다!

0.1 소셜 로그인 API ≠ 메시지 발송 API
-----------------------------------
❌ 잘못된 생각: "현재 사용 중인 카카오/네이버 API 키로 메시지도 보낼 수 있겠지?"
✅ 정답: "완전히 다른 플랫폼이므로 별도 가입 및 API 키 발급 필요!"

현재 프로젝트 상황:
```env
# 현재 .env.local에 있는 소셜 로그인용 키
KAKAO_APP_KEY=0533b523...           # ← 로그인 전용!
NAVER_CLIENT_ID=wsvDO...            # ← 로그인 전용!
NAVER_CLIENT_SECRET=Y...            # ← 로그인 전용!
```

이 키들로는 메시지 발송이 **절대 불가능**합니다!


0.2 플랫폼별 비교표
------------------

| 서비스 | 소셜 로그인 (현재 사용 중) | 메시지 발송 (구현 필요) |
|--------|--------------------------|----------------------|
| **카카오** | developers.kakao.com<br>(Kakao Developers) | business.kakao.com<br>(Kakao Business) ← **완전히 다른 사이트!** |
| **네이버** | developers.naver.com<br>(Naver Developers) | partner.talk.naver.com<br>(Naver 톡톡 파트너센터) ← **완전히 다른 사이트!** |
| **RCS** | - | rcsbizcenter.com<br>(RCS Biz Center, 이통 3사 공동 운영) |
| 가입 조건 | 앱 등록만 | **사업자 등록 필수** |
| 승인 기간 | 즉시 | 1일 ~ 2주 (RCS 가장 오래 걸림) |
| 용도 | 사용자 인증 | 고객에게 메시지 발송 |
| 과금 | 무료 | 건당 8~30원 |

⚠️ **핵심:** 같은 회사(카카오/네이버)라도 로그인 API와 메시지 API는 **완전히 별개!**
⚠️ **핵심:** 같은 계정에서 추가 설정으로 메시지 발송 불가! **새로 가입 필요!**


0.3 구현 전 필수 작업 (예상 소요: 2-3주)
---------------------------------------

⚠️ 이 프로젝트는 직접 연동 방식으로 구현합니다.

필수 준비 사항:
--------------

[ ] 사업자 등록증 준비

[ ] 1단계: 카카오 비즈니스 가입 (1주 소요)
    └─ https://business.kakao.com/ 접속
    └─ 카카오톡 채널 개설
    └─ 발신 프로필 등록 및 심사 (1-3일)
    └─ 알림톡 템플릿 등록 및 심사 (각 2-3일)
    └─ Sender Key 발급 받기

[ ] 2단계: 네이버 톡톡 파트너 가입 (즉시)
    └─ https://partner.talk.naver.com/ 접속
    └─ 톡톡 서비스 생성
    └─ 템플릿 등록
    └─ Partner Key 발급 받기

[ ] 3단계: RCS Biz Center 가입 (2주 소요) ⚠️ 가장 오래 걸림!
    └─ https://www.rcsbizcenter.com/ 접속
    └─ 브랜드 개설 신청
    └─ 브랜드 심사 대기 (1-2주)
    └─ 대행사 지정 ("솔라피 주식회사" 등)
    └─ 대화방 등록 (발신번호 등록)
    └─ Brand ID 발급 받기


0.4 구현 시작 전 체크리스트
--------------------------

이 계획서로 구현하기 전에 반드시 확인하세요:

[ ] 카카오 비즈니스 계정 준비 완료?
    → 채널 개설 완료?
    → Sender Key 발급 완료?
    → 최소 1개 이상 알림톡 템플릿 승인 완료?

[ ] 네이버 톡톡 파트너 계정 준비 완료?
    → 톡톡 서비스 생성 완료?
    → Partner Key 발급 완료?
    → 템플릿 등록 완료?

[ ] RCS Biz Center 계정 준비 완료?
    → 브랜드 심사 승인 완료? (1-2주 소요)
    → 대행사 지정 완료?
    → Brand ID 발급 완료?

⚠️ 위 3가지가 모두 준비되지 않으면 구현 시작 불가!


0.5 최종 확인
------------

✅ 이해했습니다: 소셜 로그인 API와 메시지 발송 API는 완전히 다릅니다.
✅ 이해했습니다: 카카오/네이버/RCS 각각 별도 플랫폼 가입이 필요합니다.
✅ 준비했습니다: 카카오 비즈니스 Sender Key 발급 완료
✅ 준비했습니다: 네이버 톡톡 Partner Key 발급 완료
✅ 준비했습니다: RCS Brand ID 발급 완료 (또는 심사 진행 중)

위 사항을 모두 확인한 후 구현을 시작하세요!


================================================================================
1. 프로젝트 개요 및 현황
================================================================================

1.1 구현 대상
-------------
⚠️ 중요: 이 계획서는 **완성된 UI를 절대 수정하지 않고** 백엔드 로직만 구현합니다.

✅ 현재 완전 구현됨:
   - MessageSendTab 좌측 영역 (발신번호/수신번호 관리)
   - SMS 하위 탭 (SmsMessageContent) - 전체 기능

❌ UI만 있고 로직 구현 필요:
   1. MessageSendTab 내 3개 하위 탭 (발송용 UI)
      - 💬 카카오톡 (KakaoMessageContent)
      - 🔵 RCS (RcsMessageContent)
      - 🟢 네이버 톡톡 (NaverTalkContent)

   2. KakaoNaverRcsTab (채널/템플릿 관리용)
      - 카카오: 3개 하위 탭
      - RCS: 2개 하위 탭
      - 네이버: 2개 하위 탭

1.2 현재 상태
-------------
✅ UI 완성: 모든 컴포넌트 100% 완성
✅ SMS 발송: Naver SENS API 완전 구현
❌ 카카오 API: 미설정
❌ RCS API: 미설정
❌ 네이버 톡톡 API: 미설정
❌ 백엔드 로직: 전체 미구현
❌ 채널 연동: 미완료

1.3 구현 난이도
-------------
⭐⭐⭐⭐⭐ (매우 높음)

복잡도 요인:
1. 외부 API 연동 필요 (각 채널별 인증/승인 절차)
2. 템플릿 관리 시스템 (사전 승인 필수)
3. 이미지/미디어 처리 (채널별 규격 상이)
4. 버튼/액션 관리 (채널별 제약 상이)


================================================================================
2. UI 구조 완전 분석
================================================================================

2.1 전체 페이지 구조
-------------------
📱 /messages/send 페이지

├─ 탭 1: "메시지 보내기" (MessageSendTab)
│  │
│  ├─ 좌측 영역 (모든 하위 탭 공통) ✅ 완전 구현됨
│  │  ├─ 메시지 발신번호
│  │  ├─ 메시지 수신번호 (주소록/엑셀/텍스트 업로드)
│  │  ├─ 추가한 수신번호 목록
│  │  └─ 저장/불러오기
│  │
│  └─ 우측 영역 - 4개 하위 탭
│     ├─ 📱 문자메시지 (SmsMessageContent) ✅ 완전 구현됨
│     ├─ 💬 카카오톡 (KakaoMessageContent) ❌ 로직 필요
│     ├─ 🔵 RCS 문자 (RcsMessageContent) ❌ 로직 필요
│     └─ 🟢 네이버 톡톡 (NaverTalkContent) ❌ 로직 필요
│
└─ 탭 2: "카카오/네이버/RCS" (KakaoNaverRcsTab)
   │
   ├─ 메인 탭 1: 카카오
   │  ├─ 채널/그룹 (KakaoChannelTab) ❌ 로직 필요
   │  ├─ 알림톡 템플릿 (KakaoAlimtalkTab) ❌ 로직 필요
   │  └─ 브랜드 템플릿 (KakaoBrandTab) ❌ 로직 필요
   │
   ├─ 메인 탭 2: RCS
   │  ├─ RCS 브랜드 (RcsBrandTab) ❌ 로직 필요
   │  └─ RCS 템플릿 (RcsTemplateTab) ❌ 로직 필요
   │
   └─ 메인 탭 3: 네이버톡톡
      ├─ 톡톡 아이디 (NaverTalkIdTab) ❌ 로직 필요
      └─ 네이버 템플릿 (NaverTemplateTab) ❌ 로직 필요


2.2 MessageSendTab - 카카오톡 (KakaoMessageContent)
----------------------------------------------------
파일: src/components/messages/KakaoMessageContent.tsx

3개 하위 탭:

1️⃣ 알림톡 (alimtalk)
   - 카카오 채널 선택 (드롭다운)
     └─ "연동된 채널이 없습니다" → "채널 연동하기 ＞" 버튼
   - 알림톡 템플릿 선택 (드롭다운)
     └─ "먼저 카카오 채널을 선택해주세요" → "선택" 버튼
   - 템플릿 내용 표시 (textarea, 읽기전용)
   - 문구 치환 섹션
     └─ "내용에 변수가 없습니다" (Info 아이콘)
   - 발송실패 시 문자대체발송 여부 (체크박스)

2️⃣ 친구톡 (friendtalk)
   - 카카오 채널 선택 (드롭다운)
   - 메시지 내용 입력 (textarea, 최대 1,000자)
     └─ 하단 도구바:
        - 아이콘 버튼: 치환문구, 이미지 첨부, 저장
        - 텍스트 버튼: 저장내용, 최근발송
        - 우측: 글자수 표시
   - 이미지 첨부 영역 (showImageUpload = true 시)
     └─ 가이드: 500x250px 이상, 500KB 이하, JPG/PNG
     └─ 업로드 영역
     └─ 이미지 링크 URL (최대 100자)
   - 카카오톡 버튼
     └─ "친구톡 버튼 추가" (점선 박스)
   - 문구 치환
     └─ 변수 개수 표시
   - 발송 옵션
     └─ 광고메시지 여부 (체크박스)
     └─ 발송실패 시 문자대체발송 여부 (체크박스)

3️⃣ 브랜드 메시지 (brand)
   - 카카오 채널 선택
   - 브랜드 템플릿 선택
   - 템플릿 미리보기
     └─ "템플릿 등록하기 ＞" 버튼
   - 8가지 템플릿 타입 (2줄 버튼 그리드)
     └─ 1줄: 텍스트형, 이미지형, 와이드형, 와이드리스트형
     └─ 2줄: 캐러셀피드형, 커머스형, 캐러셀커머스형, 프리미엄동영상(비활성)
   - 선택된 템플릿 정보 + 예시 이미지 (2단 레이아웃)
   - 전체수신번호 섹션 (원형 차트)
   - 수신대상 탭 (전체/수신동의자만/채널친구만)
     └─ 현재: "채널친구만" 활성
   - 문구 치환

주요 상태 변수:
```typescript
const [activeKakaoTab, setActiveKakaoTab] = useState("alimtalk");
const [templateContent, setTemplateContent] = useState("");
const [friendTalkContent, setFriendTalkContent] = useState("");
const [selectedTemplate, setSelectedTemplate] = useState("wide");
const [showImageUpload, setShowImageUpload] = useState(false);
```


2.3 MessageSendTab - RCS (RcsMessageContent)
---------------------------------------------
파일: src/components/messages/RcsMessageContent.tsx

구성 요소:
- RCS 브랜드 선택 (2단 레이아웃)
  └─ 좌측: "RCS 브랜드를 선택하세요" → "연동하기 ＞" 버튼
  └─ 우측: "RCS 템플릿 없음 (내용 직접 입력)" → "등록 ＞" 버튼

- 제목 입력 (선택사항, 최대 30자)
  └─ 우측: 글자수 표시

- 메시지 내용 (textarea, 최대 1,300자)
  └─ placeholder: "이곳에 RCS 문자 내용을 입력합니다.\n치환문구 예시) {{이름}}님 {{시간}}시 방문 예약입니다."
  └─ 하단 도구바:
     - 아이콘 버튼: 치환문구, 이미지 첨부, 저장
     - 텍스트 버튼: 저장내용, 최근발송
     - 우측: 글자수 표시

- 이미지 첨부 영역 (showImageUpload = true 시)
  └─ 가이드: 568x336px 권장
  └─ 업로드 영역

- RCS 메시지 버튼 & RCS 슬라이드 (2단 레이아웃)
  └─ 좌측: RCS 메시지 버튼
     └─ "버튼 추가 (0/2)" (Plus 아이콘)
  └─ 우측: RCS 슬라이드
     └─ 3가지 타입: 사용안함 / 좁게 / 넓게
     └─ 좁게/넓게 선택 시: "슬라이드 추가 (0/5)"

- 문구 치환
  └─ 변수 개수 표시 ({{}} 형식)

- 발송 옵션 (3개 체크박스)
  └─ 광고메시지 여부
  └─ 발송실패 시 문자대체발송 여부 (기본 체크)
  └─ 메시지 공유가능여부 (기본 체크)

주요 상태 변수:
```typescript
const [subjectLength, setSubjectLength] = useState(0);
const [messageLength, setMessageLength] = useState(0);
const [messageContent, setMessageContent] = useState("");
const [selectedSlideType, setSelectedSlideType] = useState("none");
const [showImageUpload, setShowImageUpload] = useState(false);
```


2.4 MessageSendTab - 네이버 톡톡 (NaverTalkContent)
---------------------------------------------------
파일: src/components/messages/NaverTalkContent.tsx

구성 요소:
- 상단 정보 바 (회색 배경)
  └─ "[네이버 스마트알림 발송방법 및 수신확인 문의 : 1577-1603]"

- 네이버톡 선택 & 템플릿 선택 (2단 레이아웃)
  └─ 좌측: 네이버톡 선택 (드롭다운)
  └─ 우측: 템플릿 선택 (드롭다운)

- 템플릿 내용 (회색 배경 영역)
  └─ placeholder: "사용할 템플릿을 선택하면, 이곳에 템플릿 내용이 표시됩니다. (내용수정불가)"

- 문구 치환
  └─ 파란색 배경 박스
  └─ "내용에 변수가 없습니다"

- 발송실패 시 문자대체발송 여부 (체크박스, 기본 체크)

주요 상태 변수:
```typescript
const [templateContent, setTemplateContent] = useState("");
```


2.5 KakaoNaverRcsTab - 카카오 채널/그룹 (KakaoChannelTab)
---------------------------------------------------------
파일: src/components/messages/kakao/KakaoChannelTab.tsx

2단 레이아웃:

좌측 영역 - 카카오 채널
- 헤더: "카카오 채널" + "채널 연동" 버튼 (Plus 아이콘, 파란색)
- 검색: "채널 검색" (Search 아이콘)
- 빈 상태: "채널이 존재하지 않습니다."

우측 영역 - 카카오 채널 그룹
- 헤더: "카카오 채널 그룹" (HelpCircle 아이콘)
  └─ "그룹 초대 내역" 버튼 (Mail 아이콘)
  └─ "그룹 생성" 버튼 (Plus 아이콘, 파란색)
- 검색: "그룹 검색"
- 빈 상태: "채널 그룹이 존재하지 않습니다."


2.6 KakaoNaverRcsTab - 알림톡 템플릿 (KakaoAlimtalkTab)
-------------------------------------------------------
파일: src/components/messages/kakao/KakaoAlimtalkTab.tsx

2단 레이아웃:

좌측 영역 - 알림톡 템플릿
- 헤더: "알림톡 템플릿" + "템플릿 등록" 버튼 (Plus 아이콘, 파란색)
- 검색: "템플릿 이름, ID, 검색용 ID 검색" (ChevronDown 아이콘)
- 빈 상태: "템플릿이 존재하지 않습니다."

우측 영역 - 템플릿 미리보기
- 헤더: "템플릿 미리보기"
- 빈 상태: "템플릿을 선택하면 미리보기가 표시됩니다."


2.7 KakaoNaverRcsTab - 브랜드 템플릿 (KakaoBrandTab)
----------------------------------------------------
파일: src/components/messages/kakao/KakaoBrandTab.tsx

2단 레이아웃:

좌측 영역 - 브랜드 템플릿
- 헤더: "브랜드 템플릿" (HelpCircle 아이콘) + "템플릿 등록" 버튼
- 검색: "템플릿 이름, ID, 채널명 검색" (ChevronDown 아이콘)
- 빈 상태: "브랜드 템플릿이 존재하지 않습니다."

우측 영역 - 템플릿 미리보기
- 헤더: "템플릿 미리보기"
- 빈 상태: "템플릿을 선택하면 미리보기가 표시됩니다."


2.8 KakaoNaverRcsTab - RCS 브랜드 (RcsBrandTab)
-----------------------------------------------
파일: src/components/messages/rcs/RcsBrandTab.tsx

단일 컬럼 레이아웃 + 우측 이미지:

- RCS 메시지 소개 (회색 배경 박스)
  └─ "RCS 자세히 알아보기 ↗" 버튼
  └─ "RCS 메시지 미리보기 ↗" 버튼

- 1. 브랜드 개설 (회색 배경 박스)
  └─ 설명 + "브랜드 개설 가이드 ↗" 버튼

- 2. "솔라피 주식회사" 브랜드 대행사 지정
  └─ 대행사 지정 방법 (파란색 배경 박스, 7단계)
  └─ "브랜드 운영권한 가이드 ↗" 버튼

- 3. 대화방 등록
  └─ 설명

- 4. 브랜드 솔라피와 연동
  └─ "RCS 브랜드 연동" 버튼 (파란색)

- 우측: RCS 슬라이드형 미리보기 이미지

- 하단: "연동된 RCS 브랜드가 없습니다."


2.9 KakaoNaverRcsTab - RCS 템플릿 (RcsTemplateTab)
--------------------------------------------------
파일: src/components/messages/rcs/RcsTemplateTab.tsx

구성:
- 검색 필터 (4단 그리드)
  └─ 브랜드 선택 (드롭다운)
  └─ 템플릿 이름 (input)
  └─ 템플릿 ID (input)
  └─ 검색 / 검색 초기화 버튼

- 액션 버튼 (상단)
  └─ 좌측: "RCS 템플릿 생성" (파란색) + "RCS 승인 템플릿 가져오기" (테두리)
  └─ 우측: 페이지네이션 (20개 / 1/1 / 좌우 화살표)

- 템플릿 테이블 (6컬럼)
  └─ 헤더: 템플릿 이름 / 템플릿 아이디 / 형태 / 상태 / 최근 수정일 / 템플릿 관리
  └─ 빈 상태: "RCS 템플릿 목록이 없습니다."

- 액션 버튼 (하단 복제)
- 페이지네이션 (하단 복제)


2.10 KakaoNaverRcsTab - 톡톡 아이디 (NaverTalkIdTab)
----------------------------------------------------
파일: src/components/messages/naver/NaverTalkIdTab.tsx

구성:
- 액션 버튼 (상단)
  └─ 좌측: "네이버톡톡 연동방법 ↗" (녹색) + "네이버 톡톡 파트너센터 ↗"
  └─ 우측: 페이지네이션

- 톡톡 아이디 테이블 (5컬럼)
  └─ 헤더: 톡 이름 / Talk ID / 고유 ID / 연동일 / 템플릿 관리
  └─ 빈 상태: "목록이 없습니다."

- 액션 버튼 (하단 복제)
- 페이지네이션 (하단 복제)


2.11 KakaoNaverRcsTab - 네이버 템플릿 (NaverTemplateTab)
---------------------------------------------------------
파일: src/components/messages/naver/NaverTemplateTab.tsx

구성:
- 검색 필터 (6단 그리드)
  └─ 연동 에이전트 (드롭다운)
  └─ 템플릿 상태 (드롭다운)
  └─ 템플릿 이름 (input)
  └─ 템플릿 ID (input)
  └─ "숨긴 템플릿 보기" (체크박스)
  └─ 검색 / 검색 초기화 버튼

- 액션 버튼 (상단)
  └─ 좌측: "네이버 템플릿 생성" (녹색)
  └─ 우측: 페이지네이션

- 템플릿 테이블 (6컬럼)
  └─ 헤더: 템플릿 이름 / 템플릿 아이디 / 상태 / 버튼 수 / 최근 수정일 / 템플릿 관리
  └─ 빈 상태: "목록이 없습니다."

- 액션 버튼 (하단 복제)
- 페이지네이션 (하단 복제)


================================================================================
3. 필요한 외부 API 및 환경변수
================================================================================

⚠️ 이 프로젝트는 직접 연동 방식으로 구현합니다.

3.1 카카오 비즈니스 메시지 API
----------------------------

필요한 환경변수:
```env
# 카카오 알림톡/친구톡/브랜드
KAKAO_BIZM_API_KEY=your_rest_api_key
KAKAO_BIZM_SENDER_KEY=your_sender_key        # 발신 프로필 키 (채널별로 발급)
KAKAO_BIZM_API_URL=https://kapi.kakao.com/v2/api/talk/memo/default/send
KAKAO_CHANNEL_ID=your_channel_id             # 카카오톡 채널 ID
KAKAO_CHANNEL_UUID=your_channel_uuid         # 카카오 채널 고유 ID
```

발급 방법:
1. https://business.kakao.com/ 접속
2. 카카오톡 채널 개설
3. 발신 프로필 등록 및 심사 (1-3일)
4. "관리 → 발신 프로필" 메뉴에서 Sender Key 확인
5. 알림톡 템플릿 등록 후 승인 대기 (2-3일)

공식 문서:
- https://developers.kakao.com/docs/latest/ko/message/rest-api
- https://business.kakao.com/info/bizmessage/


3.2 RCS (Rich Communication Services) API
-----------------------------------------

필요한 환경변수:
```env
# RCS Biz Center
RCS_API_KEY=your_api_key
RCS_BRAND_ID=your_brand_id              # 브랜드 심사 후 발급
RCS_CHATBOT_ID=your_chatbot_id          # 챗봇 ID
RCS_API_URL=https://rcs-api.example.com

# 이통사별 RCS (필요 시)
KT_RCS_API_KEY=your_kt_key
SK_RCS_API_KEY=your_sk_key
LG_RCS_API_KEY=your_lg_key
```

발급 방법:
1. https://www.rcsbizcenter.com/ 접속 및 가입
2. 기업 대시보드 → 브랜드 개설
3. 브랜드 정보 입력, 퀵 버튼 설정
4. 브랜드 심사 대기 (1-2주)
5. 승인 후 브랜드 ID 발급
6. 대행사 지정 ("솔라피 주식회사" 등)
7. 대화방 등록 (발신번호 필수)

공식 문서:
- https://www.rcsbizcenter.com/
- RCS 표준 문서: GSMA RCS Universal Profile


3.3 네이버 톡톡 (스마트알림) API
------------------------------

필요한 환경변수:
```env
# 네이버 톡톡
NAVER_TALKTALK_API_KEY=your_api_key
NAVER_TALKTALK_SECRET_KEY=your_secret_key
NAVER_TALKTALK_PARTNER_ID=your_partner_id
NAVER_TALKTALK_SERVICE_ID=your_service_id
NAVER_TALKTALK_API_URL=https://talk.naver.com/api/v1
```

발급 방법:
1. https://partner.talk.naver.com/ 접속
2. 파트너 가입 (사업자 등록 필요)
3. 서비스 등록
4. API Key 발급 (파트너센터 → 설정 → API 관리)
5. 스마트알림 템플릿 등록

공식 문서:
- https://partner.talk.naver.com/
- https://developers.naver.com/docs/talktalk/


3.4 최종 .env.local 파일 예시
-----------------------------

```env
# ===== 기존 소셜 로그인 (그대로 유지) =====
KAKAO_APP_KEY=0533b523...               # 소셜 로그인 전용
NAVER_CLIENT_ID=wsvDO...                # 소셜 로그인 전용
NAVER_CLIENT_SECRET=Y...                # 소셜 로그인 전용

# ===== 카카오 비즈니스 메시지 (새로 추가) =====
KAKAO_BIZM_API_KEY=your_rest_api_key
KAKAO_BIZM_SENDER_KEY=your_sender_key
KAKAO_BIZM_API_URL=https://kapi.kakao.com/v2/api/talk/memo/default/send
KAKAO_CHANNEL_ID=your_channel_id
KAKAO_CHANNEL_UUID=your_channel_uuid

# ===== RCS (새로 추가) =====
RCS_API_KEY=your_api_key
RCS_BRAND_ID=your_brand_id
RCS_CHATBOT_ID=your_chatbot_id
RCS_API_URL=https://rcs-api.example.com

# ===== 네이버 톡톡 (새로 추가) =====
NAVER_TALKTALK_API_KEY=your_api_key
NAVER_TALKTALK_SECRET_KEY=your_secret_key
NAVER_TALKTALK_PARTNER_ID=your_partner_id
NAVER_TALKTALK_SERVICE_ID=your_service_id
```

⚠️ 주의: 소셜 로그인 키와 메시지 발송 키는 완전히 별개입니다!


================================================================================
4. 데이터베이스 스키마
================================================================================

4.1 카카오 채널 정보 (kakao_channels)
------------------------------------
```sql
CREATE TABLE kakao_channels (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  channel_id VARCHAR(100) NOT NULL UNIQUE,
  channel_name VARCHAR(200) NOT NULL,
  sender_key VARCHAR(100),
  category VARCHAR(50),
  status VARCHAR(20) DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_kakao_channels_user_id ON kakao_channels(user_id);
CREATE INDEX idx_kakao_channels_status ON kakao_channels(status);
```


4.2 카카오 채널 그룹 (kakao_channel_groups)
------------------------------------------
```sql
CREATE TABLE kakao_channel_groups (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  group_name VARCHAR(200) NOT NULL,
  description TEXT,
  channel_ids INTEGER[] DEFAULT '{}', -- 그룹에 속한 채널 ID 배열
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_kakao_groups_user_id ON kakao_channel_groups(user_id);
```


4.3 카카오 템플릿 (kakao_templates)
----------------------------------
```sql
CREATE TABLE kakao_templates (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  channel_id INTEGER REFERENCES kakao_channels(id),
  template_type VARCHAR(20) NOT NULL, -- 'alimtalk', 'friendtalk', 'brand'
  template_code VARCHAR(100), -- 알림톡 템플릿 코드 (NHN Cloud 발급)
  template_name VARCHAR(200) NOT NULL,
  template_content TEXT NOT NULL,
  template_variables JSONB DEFAULT '[]', -- 변수 목록 [{name: "고객명", type: "string"}]
  buttons JSONB DEFAULT '[]', -- 버튼 정보
  image_url TEXT, -- 브랜드/친구톡 이미지
  brand_template_type VARCHAR(50), -- 'text', 'image', 'wide', 'widelist', etc.
  status VARCHAR(20) DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  rejection_reason TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_kakao_templates_user_id ON kakao_templates(user_id);
CREATE INDEX idx_kakao_templates_type ON kakao_templates(template_type);
CREATE INDEX idx_kakao_templates_status ON kakao_templates(status);
```


4.4 RCS 브랜드 정보 (rcs_brands)
-------------------------------
```sql
CREATE TABLE rcs_brands (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  brand_id VARCHAR(100) NOT NULL UNIQUE,
  brand_name VARCHAR(200) NOT NULL,
  chatbot_id VARCHAR(100),
  agency_name VARCHAR(100) DEFAULT '솔라피 주식회사',
  status VARCHAR(20) DEFAULT 'pending', -- 'pending', 'approved', 'rejected'
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_rcs_brands_user_id ON rcs_brands(user_id);
CREATE INDEX idx_rcs_brands_status ON rcs_brands(status);
```


4.5 RCS 템플릿 (rcs_templates)
-----------------------------
```sql
CREATE TABLE rcs_templates (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  brand_id INTEGER REFERENCES rcs_brands(id),
  template_id VARCHAR(100), -- RCS 템플릿 ID
  template_name VARCHAR(200) NOT NULL,
  template_type VARCHAR(50), -- 'standalone', 'carousel_narrow', 'carousel_wide'
  template_content TEXT,
  message_base_id VARCHAR(100),
  buttons JSONB DEFAULT '[]',
  slides JSONB DEFAULT '[]',
  media_url TEXT,
  status VARCHAR(20) DEFAULT 'pending',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_rcs_templates_user_id ON rcs_templates(user_id);
CREATE INDEX idx_rcs_templates_brand_id ON rcs_templates(brand_id);
CREATE INDEX idx_rcs_templates_status ON rcs_templates(status);
```


4.6 네이버 톡톡 서비스 (naver_talk_services)
------------------------------------------
```sql
CREATE TABLE naver_talk_services (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  partner_id VARCHAR(100),
  service_id VARCHAR(100) UNIQUE,
  service_name VARCHAR(200),
  talk_id VARCHAR(100), -- Talk ID
  unique_id VARCHAR(100), -- 고유 ID
  status VARCHAR(20) DEFAULT 'pending',
  linked_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_naver_services_user_id ON naver_talk_services(user_id);
```


4.7 네이버 톡톡 템플릿 (naver_talk_templates)
-------------------------------------------
```sql
CREATE TABLE naver_talk_templates (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  service_id INTEGER REFERENCES naver_talk_services(id),
  template_id VARCHAR(100), -- 네이버 템플릿 ID
  template_code VARCHAR(100),
  template_name VARCHAR(200) NOT NULL,
  template_content TEXT NOT NULL,
  template_variables JSONB DEFAULT '[]',
  button_count INTEGER DEFAULT 0,
  status VARCHAR(20) DEFAULT 'pending',
  is_hidden BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_naver_templates_user_id ON naver_talk_templates(user_id);
CREATE INDEX idx_naver_templates_service_id ON naver_talk_templates(service_id);
CREATE INDEX idx_naver_templates_status ON naver_talk_templates(status);
```


4.8 통합 메시지 발송 로그 (multi_channel_message_logs)
-----------------------------------------------------
```sql
CREATE TABLE multi_channel_message_logs (
  id SERIAL PRIMARY KEY,
  user_id INTEGER NOT NULL REFERENCES users(id),
  channel_type VARCHAR(30) NOT NULL, -- 'kakao_alimtalk', 'kakao_friendtalk', 'kakao_brand', 'rcs', 'naver_talk'
  template_id INTEGER, -- 사용된 템플릿 ID
  to_number VARCHAR(20) NOT NULL,
  to_name VARCHAR(100),
  message_content TEXT NOT NULL,
  subject VARCHAR(100),
  sent_at TIMESTAMP,
  status VARCHAR(20) NOT NULL, -- 'sent', 'failed', 'pending'
  error_message TEXT,
  credit_used INTEGER,
  fallback_to_sms BOOLEAN DEFAULT false, -- 대체발송 여부
  metadata JSONB, -- 채널별 추가 정보 (버튼, 이미지 등)
  created_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_multi_channel_logs_user_id ON multi_channel_message_logs(user_id);
CREATE INDEX idx_multi_channel_logs_channel ON multi_channel_message_logs(channel_type);
CREATE INDEX idx_multi_channel_logs_sent_at ON multi_channel_message_logs(sent_at);
CREATE INDEX idx_multi_channel_logs_status ON multi_channel_message_logs(status);
```


================================================================================
5. API 엔드포인트 설계
================================================================================

5.1 카카오톡 관리 API
--------------------

📍 POST /api/kakao/channels
설명: 카카오 채널 등록
요청:
```json
{
  "channelId": "string",
  "channelName": "string",
  "senderKey": "string"
}
```
응답:
```json
{
  "success": true,
  "channel": { ... }
}
```

📍 GET /api/kakao/channels
설명: 카카오 채널 목록 조회
쿼리: ?search=검색어
응답:
```json
{
  "channels": [ ... ],
  "total": 0
}
```

📍 DELETE /api/kakao/channels/:id
설명: 카카오 채널 삭제

📍 POST /api/kakao/channel-groups
설명: 카카오 채널 그룹 생성
요청:
```json
{
  "groupName": "string",
  "description": "string",
  "channelIds": [1, 2, 3]
}
```

📍 GET /api/kakao/channel-groups
설명: 카카오 채널 그룹 목록 조회

📍 DELETE /api/kakao/channel-groups/:id
설명: 그룹 삭제

📍 POST /api/kakao/templates
설명: 카카오 템플릿 등록
요청:
```json
{
  "channelId": 1,
  "templateType": "alimtalk",
  "templateName": "string",
  "templateContent": "string",
  "buttons": [ ... ]
}
```

📍 GET /api/kakao/templates
설명: 템플릿 목록 조회
쿼리: ?type=alimtalk&search=검색어&status=approved

📍 GET /api/kakao/templates/:id
설명: 템플릿 상세 조회

📍 DELETE /api/kakao/templates/:id
설명: 템플릿 삭제

📍 POST /api/kakao/send/alimtalk
설명: 알림톡 발송
요청:
```json
{
  "templateCode": "string",
  "recipients": [
    {
      "phone_number": "01012345678",
      "name": "홍길동",
      "variables": { "고객명": "홍길동" }
    }
  ],
  "fallbackToSms": true
}
```

📍 POST /api/kakao/send/friendtalk
설명: 친구톡 발송
요청:
```json
{
  "channelId": 1,
  "recipients": [ ... ],
  "message": "string",
  "imageUrl": "string (optional)",
  "buttons": [ ... ],
  "isAd": true
}
```

📍 POST /api/kakao/send/brand
설명: 브랜드 메시지 발송

📍 POST /api/kakao/upload/image
설명: 카카오 이미지 업로드


5.2 RCS 관리 API
---------------

📍 POST /api/rcs/brands
설명: RCS 브랜드 등록

📍 GET /api/rcs/brands
설명: RCS 브랜드 목록 조회

📍 DELETE /api/rcs/brands/:id
설명: 브랜드 삭제

📍 POST /api/rcs/templates
설명: RCS 템플릿 등록
요청:
```json
{
  "brandId": 1,
  "templateName": "string",
  "templateType": "standalone",
  "content": "string",
  "buttons": [ ... ],
  "slides": [ ... ]
}
```

📍 GET /api/rcs/templates
설명: RCS 템플릿 목록 조회
쿼리: ?brandId=1&search=검색어

📍 DELETE /api/rcs/templates/:id
설명: 템플릿 삭제

📍 POST /api/rcs/send
설명: RCS 메시지 발송
요청:
```json
{
  "brandId": 1,
  "recipients": [ ... ],
  "subject": "string",
  "message": "string",
  "buttons": [ ... ],
  "mediaUrl": "string (optional)",
  "fallbackToSms": true
}
```

📍 POST /api/rcs/upload/media
설명: RCS 미디어 업로드


5.3 네이버 톡톡 관리 API
---------------------

📍 POST /api/naver-talk/services
설명: 네이버 톡톡 서비스 등록
요청:
```json
{
  "partnerId": "string",
  "serviceId": "string",
  "serviceName": "string",
  "talkId": "string"
}
```

📍 GET /api/naver-talk/services
설명: 네이버 톡톡 서비스 목록

📍 DELETE /api/naver-talk/services/:id
설명: 서비스 삭제

📍 POST /api/naver-talk/templates
설명: 네이버 템플릿 등록

📍 GET /api/naver-talk/templates
설명: 템플릿 목록 조회
쿼리: ?serviceId=1&status=approved&search=검색어&showHidden=true

📍 PATCH /api/naver-talk/templates/:id
설명: 템플릿 숨김/표시 토글

📍 DELETE /api/naver-talk/templates/:id
설명: 템플릿 삭제

📍 POST /api/naver-talk/send
설명: 네이버 스마트알림 발송
요청:
```json
{
  "templateCode": "string",
  "recipients": [ ... ],
  "variables": { ... },
  "fallbackToSms": true
}
```


================================================================================
6. 단계별 구현 계획 (UI 기반)
================================================================================

⚠️ 이 계획은 완성된 UI를 기반으로 백엔드만 구현합니다.

Phase 1: 기반 작업 및 환경 설정 (1주)
------------------------------------
[ ] 카카오 비즈니스 계정 확인
    └─ Sender Key, Channel ID 확보
    └─ 최소 1개 알림톡 템플릿 승인 완료
[ ] 네이버 톡톡 파트너 계정 확인
    └─ Partner Key, Service ID 확보
    └─ 템플릿 등록 완료
[ ] RCS Biz Center 계정 확인
    └─ Brand ID 확보
    └─ 브랜드 심사 승인 완료 (또는 진행 중)
[ ] 환경변수 설정 (.env.local)
    └─ 카카오/네이버/RCS API 키 모두 추가
[ ] DB 마이그레이션 파일 생성 (8개 테이블)
[ ] Supabase에 테이블 생성
[ ] 기본 API 구조 설계 및 유틸리티 함수 작성
    └─ 카카오 API 클라이언트 함수
    └─ RCS API 클라이언트 함수
    └─ 네이버 톡톡 API 클라이언트 함수


Phase 2: 카카오 채널 관리 (1주)
------------------------------
KakaoChannelTab 로직 구현:

[ ] POST /api/kakao/channels - 채널 등록
[ ] GET /api/kakao/channels - 채널 목록 조회
[ ] DELETE /api/kakao/channels/:id - 채널 삭제
[ ] UI 연동: 채널 연동 버튼 클릭 시 모달 or 외부 링크
[ ] UI 연동: 검색 기능
[ ] UI 연동: 채널 목록 테이블 렌더링

[ ] POST /api/kakao/channel-groups - 그룹 생성
[ ] GET /api/kakao/channel-groups - 그룹 목록 조회
[ ] DELETE /api/kakao/channel-groups/:id - 그룹 삭제
[ ] UI 연동: 그룹 생성 모달
[ ] UI 연동: 그룹 초대 내역 모달
[ ] UI 연동: 그룹 검색 및 목록


Phase 3: 카카오 알림톡 (1주)
--------------------------
KakaoAlimtalkTab 로직 구현:

[ ] POST /api/kakao/templates - 알림톡 템플릿 등록
[ ] GET /api/kakao/templates?type=alimtalk - 템플릿 목록
[ ] GET /api/kakao/templates/:id - 템플릿 상세
[ ] DELETE /api/kakao/templates/:id - 템플릿 삭제
[ ] UI 연동: 템플릿 등록 모달
[ ] UI 연동: 검색 필터
[ ] UI 연동: 템플릿 목록 렌더링
[ ] UI 연동: 우측 미리보기 영역

KakaoMessageContent (알림톡 탭) 발송 로직:

[ ] 채널 드롭다운 - API 연동
[ ] 템플릿 드롭다운 - API 연동
[ ] 템플릿 내용 자동 로드
[ ] 변수 치환 로직
[ ] POST /api/kakao/send/alimtalk - 발송 API
[ ] 대체발송 로직 (SMS fallback)
[ ] 발송 로그 저장


Phase 4: 카카오 친구톡 & 브랜드 (1주)
------------------------------------
KakaoMessageContent (친구톡 탭) 발송 로직:

[ ] 채널 드롭다운 - API 연동
[ ] 메시지 입력 및 글자수 제한
[ ] 치환문구 추가 기능
[ ] POST /api/kakao/upload/image - 이미지 업로드
[ ] 이미지 첨부 및 링크 URL 처리
[ ] 버튼 추가 UI 모달 구현
[ ] POST /api/kakao/send/friendtalk - 발송 API
[ ] 광고메시지 표기 자동 처리
[ ] 대체발송 로직

KakaoBrandTab 로직 구현:

[ ] POST /api/kakao/templates - 브랜드 템플릿 등록
[ ] GET /api/kakao/templates?type=brand - 목록
[ ] UI 연동: 템플릿 등록 모달 (8가지 타입 선택)
[ ] UI 연동: 검색 및 목록
[ ] UI 연동: 미리보기

KakaoMessageContent (브랜드 탭) 발송 로직:

[ ] 채널 드롭다운 연동
[ ] 템플릿 선택 (8가지 타입)
[ ] 템플릿 미리보기 이미지 로드
[ ] 수신대상 필터링 (채널친구만)
[ ] POST /api/kakao/send/brand - 발송 API


Phase 5: RCS (2주)
------------------
RcsBrandTab 로직 구현:

[ ] 브랜드 개설 가이드 외부 링크 연동
[ ] POST /api/rcs/brands - 브랜드 등록
[ ] GET /api/rcs/brands - 브랜드 목록
[ ] UI 연동: "RCS 브랜드 연동" 버튼 클릭 시 등록 플로우
[ ] UI 연동: 연동된 브랜드 목록 표시

RcsTemplateTab 로직 구현:

[ ] POST /api/rcs/templates - 템플릿 생성
[ ] GET /api/rcs/templates - 템플릿 목록
[ ] DELETE /api/rcs/templates/:id - 템플릿 삭제
[ ] UI 연동: 검색 필터 (브랜드, 이름, ID)
[ ] UI 연동: "RCS 템플릿 생성" 모달
[ ] UI 연동: "RCS 승인 템플릿 가져오기" 기능
[ ] UI 연동: 테이블 렌더링 및 페이지네이션

RcsMessageContent 발송 로직:

[ ] RCS 브랜드 드롭다운 연동
[ ] RCS 템플릿 드롭다운 연동
[ ] 제목 입력 (30자 제한)
[ ] 메시지 입력 (1,300자 제한)
[ ] 치환문구 추가 ({{}} 형식)
[ ] POST /api/rcs/upload/media - 이미지 업로드
[ ] 이미지 첨부 처리
[ ] RCS 버튼 추가 모달 구현 (최대 2개)
[ ] RCS 슬라이드 추가 모달 (좁게/넓게, 최대 5개)
[ ] POST /api/rcs/send - 발송 API
[ ] 대체발송 로직


Phase 6: 네이버 톡톡 (1주)
-------------------------
NaverTalkIdTab 로직 구현:

[ ] 외부 링크 연동 (연동방법, 파트너센터)
[ ] POST /api/naver-talk/services - 서비스 등록
[ ] GET /api/naver-talk/services - 서비스 목록
[ ] DELETE /api/naver-talk/services/:id - 서비스 삭제
[ ] UI 연동: 테이블 렌더링
[ ] UI 연동: 페이지네이션

NaverTemplateTab 로직 구현:

[ ] POST /api/naver-talk/templates - 템플릿 생성
[ ] GET /api/naver-talk/templates - 템플릿 목록
[ ] PATCH /api/naver-talk/templates/:id - 숨김 토글
[ ] DELETE /api/naver-talk/templates/:id - 삭제
[ ] UI 연동: 검색 필터 (에이전트, 상태, 이름, ID, 숨김)
[ ] UI 연동: "네이버 템플릿 생성" 모달
[ ] UI 연동: 테이블 및 페이지네이션

NaverTalkContent 발송 로직:

[ ] 네이버톡 선택 드롭다운 연동
[ ] 템플릿 선택 드롭다운 연동
[ ] 템플릿 내용 자동 로드 (읽기전용)
[ ] 변수 치환 로직
[ ] POST /api/naver-talk/send - 발송 API
[ ] 대체발송 로직


Phase 7: 통합 테스트 & 최적화 (1주)
----------------------------------
[ ] 전체 채널 발송 테스트 (알림톡, 친구톡, 브랜드, RCS, 네이버)
[ ] 대체발송 동작 검증
[ ] 에러 처리 검증
[ ] 로그 저장 확인 (multi_channel_message_logs)
[ ] 통계 대시보드 (선택사항)
[ ] 성능 최적화 (쿼리 최적화, 인덱스)
[ ] 문서화 (API 명세서)


================================================================================
7. 각 채널별 상세 구현 가이드
================================================================================

7.1 카카오 알림톡 구현
--------------------

특징:
- 사전 승인된 템플릿만 사용 가능
- 템플릿 내용 수정 불가
- 변수 치환만 가능
- 발송 성공률 높음
- 광고 아님

구현 순서:
1. 카카오 채널 연동 (KakaoChannelTab)
2. 템플릿 등록 및 심사 신청 (KakaoAlimtalkTab)
3. 승인된 템플릿 DB 저장
4. 발송 시 템플릿 코드 + 변수 전달 (KakaoMessageContent)
5. 실패 시 SMS 대체발송

코드 예시:
```typescript
// src/app/api/kakao/send/alimtalk/route.ts
import { NextRequest, NextResponse } from 'next/server';
import { createClient } from '@supabase/supabase-js';

const supabase = createClient(
  process.env.NEXT_PUBLIC_SUPABASE_URL!,
  process.env.SUPABASE_SERVICE_ROLE_KEY!
);

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { templateCode, recipients, fallbackToSms } = body;

    // JWT 인증
    const token = request.headers.get('Authorization')?.replace('Bearer ', '');
    // ... 토큰 검증 로직

    // 템플릿 조회
    const { data: template } = await supabase
      .from('kakao_templates')
      .select('*')
      .eq('template_code', templateCode)
      .eq('status', 'approved')
      .single();

    if (!template) {
      return NextResponse.json({ error: '승인되지 않은 템플릿' }, { status: 400 });
    }

    // 카카오 비즈니스 API 호출 (직접 연동)
    const results = [];
    for (const recipient of recipients) {
      try {
        const response = await fetch('https://kapi.kakao.com/v2/api/talk/memo/default/send', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Authorization': `KakaoAK ${process.env.KAKAO_BIZM_API_KEY!}`
          },
          body: JSON.stringify({
            sender_key: template.sender_key,
            template_code: templateCode,
            receiver_number: recipient.phone_number,
            template_parameter: recipient.variables
          })
        });

        const data = await response.json();

        results.push({
          recipient: recipient.phone_number,
          success: response.ok,
          messageId: data.header?.resultCode === 0 ? data.recipientList[0]?.requestId : null
        });

        // 로그 저장
        await supabase.from('multi_channel_message_logs').insert({
          user_id: userId,
          channel_type: 'kakao_alimtalk',
          template_id: template.id,
          to_number: recipient.phone_number,
          to_name: recipient.name,
          message_content: template.template_content,
          status: response.ok ? 'sent' : 'failed',
          error_message: response.ok ? null : data.header?.resultMessage,
          credit_used: 15
        });

      } catch (error) {
        results.push({
          recipient: recipient.phone_number,
          success: false
        });

        // 대체발송 로직
        if (fallbackToSms) {
          // SMS 발송 API 호출
        }
      }
    }

    return NextResponse.json({ success: true, results });
  } catch (error) {
    console.error('알림톡 발송 오류:', error);
    return NextResponse.json({ error: '발송 실패' }, { status: 500 });
  }
}
```


7.2 카카오 친구톡 구현
--------------------

특징:
- 내용 자유 작성
- 친구 추가한 사용자에게만 발송
- 이미지 첨부 가능
- 버튼 추가 가능
- 광고성 메시지 가능

구현 포인트:
1. 채널 친구 확인 필요
2. 이미지 업로드 → 카카오 이미지 서버 (Supabase Storage 사용)
3. 버튼 JSON 구조 생성
4. 광고 표기 자동 처리 ("(광고)" prefix)


7.3 RCS 구현
-----------

특징:
- 가장 복잡한 채널
- 브랜드 심사 필수 (1-2주 소요)
- 리치 미디어 지원
- 캐러셀/슬라이드 지원
- 버튼 액션 다양

구현 포인트:
1. 브랜드 등록 → 심사 대기
2. 메시지베이스 설정
3. 미디어 업로드
4. 슬라이드 JSON 생성
5. 대체발송 필수


7.4 네이버 톡톡 구현
------------------

특징:
- 알림톡과 유사
- 템플릿 사전 등록
- 발송 단가 저렴
- 네이버 앱 푸시

구현 포인트:
1. 파트너 가입
2. 템플릿 등록
3. 변수 치환
4. 대체발송


================================================================================
8. 테스트 시나리오
================================================================================

8.1 카카오 채널 관리 테스트
-------------------------
[ ] 채널 연동 버튼 클릭 → 등록 플로우
[ ] 채널 목록 조회 및 검색
[ ] 그룹 생성 → 채널 추가
[ ] 그룹 목록 조회
[ ] 채널/그룹 삭제

8.2 카카오 알림톡 테스트
-----------------------
[ ] 템플릿 등록 → 심사 대기
[ ] 템플릿 목록 조회 및 검색
[ ] 템플릿 미리보기
[ ] 발송 페이지에서 채널/템플릿 선택
[ ] 변수 매칭
[ ] 발송 테스트
[ ] 발송 실패 시 SMS 대체발송 확인
[ ] 로그 확인

8.3 카카오 친구톡 테스트
-----------------------
[ ] 채널 선택
[ ] 메시지 작성 (치환문구 포함)
[ ] 이미지 업로드
[ ] 이미지 링크 URL 입력
[ ] 버튼 추가
[ ] 광고메시지 체크 → "(광고)" 자동 추가 확인
[ ] 발송 테스트

8.4 RCS 테스트
-------------
[ ] 브랜드 등록 → 심사 대기
[ ] 브랜드 목록 조회
[ ] 템플릿 생성 (standalone/carousel)
[ ] 이미지/동영상 업로드
[ ] 슬라이드 구성 (최대 5개)
[ ] 버튼 추가 (최대 2개)
[ ] 발송 테스트
[ ] 대체발송 확인

8.5 네이버 톡톡 테스트
--------------------
[ ] 톡톡 서비스 등록
[ ] 서비스 목록 조회
[ ] 템플릿 생성
[ ] 템플릿 목록 조회 및 필터링
[ ] 숨김 처리
[ ] 변수 매칭
[ ] 발송 테스트
[ ] 대체발송 확인


================================================================================
9. 보안 및 에러 처리
================================================================================

9.1 보안
--------
- API Key 암호화 저장 (환경변수)
- 템플릿 승인 상태 검증
- 사용자별 채널 격리 (user_id 체크)
- 발송 권한 검증 (JWT)
- SQL Injection 방지 (Supabase parameterized queries)

9.2 에러 처리
------------
- 채널 연동 실패 → 명확한 에러 메시지
- 템플릿 심사 반려 → 반려 사유 표시
- 발송 실패 → 에러 로그 저장 + 대체발송
- API 오류 → Retry 로직
- 대체발송 트리거 조건 명확화


================================================================================
10. 최종 체크리스트
================================================================================

구현 전:
--------
[ ] 카카오 비즈니스 채널 개설 완료?
[ ] 카카오 Sender Key 발급 완료?
[ ] 알림톡 템플릿 최소 1개 승인 완료?
[ ] RCS 브랜드 심사 승인 완료?
[ ] RCS Brand ID 발급 완료?
[ ] 네이버 톡톡 파트너 가입 완료?
[ ] 네이버 Partner Key 발급 완료?
[ ] 테스트 발송용 전화번호 준비?

구현 중:
--------
[ ] DB 테이블 생성 (8개)?
[ ] API 엔드포인트 구현 (30+개)?
[ ] 템플릿 관리 시스템?
[ ] 이미지 업로드 처리?
[ ] 발송 로직 구현?
[ ] UI 연동?

구현 후:
--------
[ ] 전체 채널 발송 테스트?
[ ] 대체발송 동작 확인?
[ ] 에러 처리 검증?
[ ] 로그 저장 확인?
[ ] 문서화 완료?


================================================================================
11. 참고 자료
================================================================================

공식 문서:
- 카카오 비즈니스 메시지: https://business.kakao.com/
- RCS Biz Center: https://www.rcsbizcenter.com/
- 네이버 톡톡: https://talk.naver.com/


================================================================================
문서 끝
================================================================================

⚠️ UI는 절대 수정하지 않고 백엔드 로직만 구현합니다!
⚠️ 외부 API 연동 및 심사 절차가 필요합니다!
⚠️ 직접 연동 방식으로 구현합니다 (카카오/네이버/RCS 각각 별도 연동)

📌 구현 예상 기간: 최소 6-8주
📌 초기 비용: 각 채널별 발송 단가 (건당 8~30원)
📌 난이도: ⭐⭐⭐⭐⭐ (매우 높음)

📌 구현 전 필수 준비:
   - 카카오 비즈니스 Sender Key
   - 네이버 톡톡 Partner Key
   - RCS Brand ID (브랜드 심사 1-2주 소요)

================================================================================
