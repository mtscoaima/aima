# 캠페인 광고금액 로직 수정 계획서 (DB 구조 반영 업데이트)

## 1. UI 필드 변경 사항

### 1.1 삭제할 필드
- "일 최대 건수" 필드 (maxRecipients)
  - 현재 위치: 빨간 테두리 영역 "100건" 입력 필드
  - 파일: src/components/target-marketing/TargetMarketingDetail.tsx (라인 3783-3793)
  - 파일: src/components/modals/CampaignDetailModal.tsx (라인 1664-1666)
  - 관련 상태: maxRecipients, setMaxRecipients

### 1.2 추가할 필드
- **캠페인 예산** 필드 (budget - DB 필드명과 일치)
  - 위치: 기존 "일 최대 건수" 필드 위치에 배치
  - 전체 캠페인에 대한 총 예산
  - 숫자 입력 + "원" 단위 표시
  - 플레이스홀더: "200000원" (최소 단가 기준)
  - **DB 저장**: campaigns.budget (numeric 타입)

- "일 최대 광고비 제한" 필드 (dailyAdSpendLimit)
  - 위치: "캠페인 예산" 필드 바로 아래 추가
  - 하루 최대 광고비 제한
  - 숫자 입력 + "원" 단위 표시
  - 플레이스홀더: "50000원"

## 2. 데이터베이스 스키마 변경

### 2.1 campaigns 테이블 현재 구조 (✅ 확인됨)
- **기존 필드**: `budget` (numeric) - 캠페인 예산으로 사용
- **삭제 예정**: max_recipients 관련 로직 (DB에서는 제거하지 않고 사용 중단)
- **추가 필요**: daily_ad_spend_limit (DECIMAL) - 일 최대 광고비 제한

### 2.2 system_settings 테이블 (✅ 완료)
**현재 구조**: 단일 레코드 형태
```sql
CREATE TABLE system_settings (
  id INTEGER PRIMARY KEY DEFAULT 1,
  first_level_commission_rate DECIMAL(5,2) NOT NULL DEFAULT 10.0,
  nth_level_denominator INTEGER NOT NULL DEFAULT 20,
  menu_settings JSONB DEFAULT '{"main_menu": [], "admin_menu": []}',
  site_settings JSONB DEFAULT '{}',
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**현재 DB에 저장된 데이터 (site_settings JSONB 내부):**
- site_name: "MTS Message"
- site_description: "AI 기반 타깃 마케팅 메시지 서비스"
- contact_email: "support@mtsmessage.com"
- contact_phone: "1588-0000"
- footer_text: "Copyright © 2024 MTS Message. All rights reserved."
- maintenance_mode: false
- maintenance_message: "시스템 점검 중입니다. 잠시 후 다시 이용해주세요."
- portal_name: "관리자 포털"
- dormant_mode: false
- timezone: "Asia/Seoul (UTC+9)"
- external_api_key: "sk_live_xxxxxxxxxxxxxxxxxxxxxxABCD"
- minimum_campaign_price: "200000" (캠페인 최소 단가)
- default_daily_limit: "50000" (기본 일일 광고비 제한)

**별도 컬럼:**
- first_level_commission_rate: 10.0
- nth_level_denominator: 20
- menu_settings: {"main_menu": [], "admin_menu": []}

## 3. 최소 단가 검증 로직 수정

### 3.1 현재 로직
- 하드코딩된 5만원 최소값

### 3.2 변경 로직
- DB system_settings.site_settings JSONB에서 minimum_campaign_price 값 조회
- 기본값: 20만원 ("200000" - 문자열 형태로 저장됨)
- 관리자 페이지에서 수정 가능 (기존 /admin/system-settings 페이지 사용)

### 3.3 검증 규칙 (요구사항 반영)
- **캠페인 당 최소 단가**: 20만원 이상
- **20만원 이하 입력 시**: 자동으로 20만원으로 설정
- **최소 단가**: 관리자 설정에서 변경 가능

### 3.4 적용 위치
- 캠페인 생성/수정 시 검증
- 프론트엔드 실시간 검증 (입력 시 자동 조정)
- 백엔드 API 검증

## 4. 관리자 설정 페이지 수정

### 4.1 기존 페이지 사용: /admin/system-settings (✅ 이미 존재)
- **기존 system-settings 페이지의 "일반 설정" 탭 활용**
- minimum_campaign_price, default_daily_limit 설정이 이미 site_settings에 포함됨
- UI에서 해당 필드들을 캠페인 예산 관련 설정으로 표시

### 4.2 API 엔드포인트 (✅ 이미 존재)
- **기존 사용**: GET /api/admin/system-settings
- **기존 사용**: PUT /api/admin/system-settings
- site_settings JSONB 내의 minimum_campaign_price, default_daily_limit 값 관리

## 5. 잔액 차감 로직 수정

### 5.1 현재 문제점
- 승인 신청 시점에 잔액 차감
- 잔액 부족 알림의 금액 불일치

### 5.2 수정 방향 (잔액 차감 로직 변경)
- **승인 신청 시**: 잔액 차감 없음, **캠페인 전체 예산** 기준으로 잔액 부족 검증
- **승인 완료 시**: **캠페인 전체 예산 전액**을 잔액에서 차감
- **잔액 부족 시**: **캠페인 전체 예산** 금액 표시
- **기존 로직 변경**: `일 최대 건수 × 단가` → `캠페인 전체 예산`

### 5.3 수정 대상 파일
- src/services/campaignService.ts
- src/contexts/BalanceContext.tsx
- src/app/api/campaigns/route.ts
- src/app/api/admin/campaigns/[id]/approve/route.ts

## 6. 마이너스 잔액 처리

### 6.1 관리자 승인으로 인한 마이너스 잔액
- 잔액이 마이너스가 되어도 승인 진행
- 마이너스 잔액 표시 + 안내 문구

### 6.2 UI 변경사항
- 잔액 표시 컴포넌트에서 마이너스 값 처리
- 마이너스 시 경고 색상 및 안내 메시지 표시

## 7. 금액 계산 로직 업데이트

### 7.1 기존 계산 로직
```javascript
calculateTotalCost(sendPolicy, maxRecipients, adRecipientCount, unitCost)
```

### 7.2 신규 계산 로직
```javascript
calculateTotalCost(sendPolicy, budget, dailyAdSpendLimit, adRecipientCount, unitCost)
```

### 7.3 계산 방식 변경 (중요!)

#### 기존 방식:
```
가용 금액 = 일 최대 건수 × 건당 광고 단가
승인 시 차감 = 가용 금액
```

#### 신규 방식:
```
승인 시 차감 = 캠페인 전체 예산 (budget)
일 최대 광고비 제한 = 하루 소비 한도 (daily_ad_spend_limit)
```

#### UI 표시:
- **총 추천 금액** = 충전잔액 + 포인트 (기존 유지)
- **필요 금액** = 캠페인 전체 예산 (budget 필드 값)
- **최소 단가 20만원 검증** 자동 적용
- 광고 단가(건당) → 광고 단가(발송 건 당)로 이름 수정

## 8. 타입 정의 업데이트

### 8.1 targetMarketing.ts 수정
```typescript
interface Campaign {
  // 삭제
  // max_recipients?: number;

  // 기존 필드 활용
  budget?: number;  // DB의 기존 budget 필드 사용

  // 추가
  daily_ad_spend_limit?: number;
}
```

## 9. 승인 신청 시 검증 로직

### 9.1 검증 항목
- 캠페인 예산 >= 최소 단가 (20만원)
- 일 최대 광고비 제한 > 0
- 캠페인 예산 >= 일 최대 광고비 제한

### 9.2 자동 조정 로직
- 캠페인 예산이 20만원 미만 입력 시 → 자동으로 20만원으로 설정
- 실시간 입력 검증 및 자동 수정

### 9.3 에러 메시지
- "캠페인 예산이 자동으로 최소 금액 {최소단가}원으로 설정되었습니다"
- "일 최대 광고비는 0원보다 커야 합니다"
- "캠페인 예산은 일 최대 광고비보다 크거나 같아야 합니다"

## 10. UI 레이아웃 변경사항 (이미지 기반)

### 10.1 기존 → 변경
- **"일 최대 건수: 100건"** → **"캠페인 예산: 원"**
- **추가**: "일 최대 광고비 제한: 원" 필드
- **유지**: 예상단가 섹션 (캠페인(건당) 150원/건)
- **유지**: 합계 및 충전 잔액 표시 영역
- **텍스트 수정**: "광고 단가(건당)" → "광고 단가(발송 건 당)"

### 10.2 필드 배치 순서
1. 발송 정책 설정 (실시간 발송/일괄 발송)
2. 유효 기간
3. **캠페인 예산** (새로 추가)
4. **일 최대 광고비 제한** (새로 추가)
5. 예상단가 (기존 유지)
6. 합계/충전 잔액 (기존 유지)

## 11. 구현 순서 및 진행 상황

### ✅ 완료된 단계:
1. **데이터베이스 스키마 확인** (✅ 완료)
   - campaigns 테이블: budget 필드 이미 존재 (numeric 타입)
   - system_settings 테이블: 단일 레코드 구조로 복원 완료
   - 필요한 설정값들이 site_settings JSONB에 저장됨
   - **추가 필요**: daily_ad_spend_limit 컬럼 추가

### ⚠️ 되돌려야 할 단계 (처음부터 다시 구현):
2. **타입 정의 업데이트** (❌ 되돌림 필요)
   - src/types/targetMarketing.ts: Campaign 인터페이스 수정 필요
   - src/services/campaignService.ts: CreateCampaignRequest 인터페이스 수정 필요
   - 현재 파일들이 복잡해져서 깔끔하게 다시 구현 필요

3. **system_settings API 구현** (✅ 이미 존재)
   - 기존 /api/admin/system-settings 사용 가능
   - JWT 인증 및 관리자 권한 검증 이미 구현됨

4. **관리자 설정 페이지** (✅ 이미 존재)
   - 기존 /admin/system-settings 페이지 활용
   - UI에서 캠페인 예산 관련 설정 표시 추가만 필요

### 🔄 대기 중인 단계 (순서대로 구현):
5. daily_ad_spend_limit 컬럼 추가
6. 최소 단가 검증 로직 수정
7. UI 필드 변경 (일 최대 건수 → 캠페인 예산, 일 최대 광고비)
8. 금액 계산 로직 업데이트
9. 잔액 차감 로직 수정
10. 마이너스 잔액 처리 구현
11. 테스트 및 검증

### 📝 현재 상태 요약:
- **데이터베이스**: ✅ 대부분 완료
  - campaigns 테이블: budget 필드 존재, daily_ad_spend_limit 추가 필요
  - system_settings 테이블: 단일 레코드 구조로 복원됨
    - site_settings JSONB 내: minimum_campaign_price: "200000", default_daily_limit: "50000"
    - 별도 컬럼: first_level_commission_rate: 10.0, nth_level_denominator: 20
    - 기타 모든 시스템 설정값들 저장됨

- **백엔드/프론트엔드**: ❌ 모두 처음부터 다시 구현 필요
  - 기존 코드가 복잡해져서 깔끔하게 다시 시작
  - 단계별로 차근차근 구현할 예정

### 🎯 다음 작업: daily_ad_spend_limit 컬럼 추가부터 시작

## 12. 필요한 SQL 마이그레이션

### 12.1 campaigns 테이블에 daily_ad_spend_limit 컬럼 추가
```sql
ALTER TABLE campaigns
ADD COLUMN daily_ad_spend_limit DECIMAL(12,2) DEFAULT 50000.00;

COMMENT ON COLUMN campaigns.daily_ad_spend_limit IS '일 최대 광고비 제한 (원)';
```

## 13. 영향받는 파일 목록

### Frontend
- src/components/target-marketing/TargetMarketingDetail.tsx
- src/components/modals/CampaignModal.tsx
- src/components/modals/CampaignDetailModal.tsx
- src/components/campaigns/CampaignManagementTab.tsx
- src/types/targetMarketing.ts
- src/services/campaignService.ts
- src/contexts/BalanceContext.tsx
- src/hooks/useTargetMarketing.ts

### Backend API
- src/app/api/campaigns/route.ts
- src/app/api/campaigns/[id]/route.ts
- src/app/api/admin/campaigns/[id]/approve/route.ts
- src/app/api/admin/campaigns/[id]/reject/route.ts
- src/app/api/admin/system-settings/* (기존 활용)

### 관리자 페이지
- src/app/admin/system-settings/page.tsx (기존 활용)
- src/app/admin/campaigns/page.tsx

### 데이터베이스
- migrations/add_daily_ad_spend_limit.sql (신규)